# Experimental decision-record v0.2 fixture

This synthetic, non-normative fixture accompanies [`docs/decision-record-draft.md`](../../docs/decision-record-draft.md). It is not Interop Profile v0.3 and does not define a schema.

After building, verify the v0.2 receipt, its fixture-specific semantics, and signed-field tamper cases:

```sh
npm run example:decision-record-v0.2-draft
```

The bundled public key is test-only. Source decision records and the evidence bundle remain hypothetical; signature verification does not prove their truth or completeness.

